<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_main_loading">

    <!--android:src="@drawable/ic_main"-->
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fabtn_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        app:backgroundTint="@color/win_medium_red"
        app:borderWidth="0dp"
        app:elevation="0dp"
        app:fabSize="auto"/>

    <TextView
        android:id="@+id/tv_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center"
        android:layout_marginBottom="@dimen/main_loading_label_margin_bottom"
        android:text="@string/msg_main_loading"
        android:textColor="@android:color/white"
        android:textSize="@dimen/main_loading_label_text_size"/>

</RelativeLayout>
